## 1. Подготовка окружения

- [x] 1.1 Удалить `package-lock.json` из репозитория
    - **Проверка**: файл `package-lock.json` отсутствует в корне проекта

- [x] 1.2 Запустить `bun install` для создания `bun.lock`
    - **Проверка**: файл `bun.lock` создан в корне проекта

## 2. Проверка совместимости

- [x] 2.1 Запустить `bun run dev` и убедиться, что dev-сервер Vite запускается
    - **Проверка**: dev-сервер стартует без ошибок, доступен на localhost

- [x] 2.2 Запустить `bun run build` и убедиться, что сборка проходит успешно
    - **Проверка**: папка `dist/` создаётся с собранными файлами

## 3. Документация

- [x] 3.1 Обновить README.md с инструкциями по использованию bun
    - **Проверка**: README содержит команды `bun install`, `bun run dev`, `bun run build`

## 4. Очистка

- [x] 4.1 Удалить `node_modules/` и выполнить `bun install` заново для чистоты теста
    - **Проверка**: `bun run dev` и `bun run build` работают после чистой установки
